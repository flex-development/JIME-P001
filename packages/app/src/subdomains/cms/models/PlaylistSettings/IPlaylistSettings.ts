import { IEntity } from '@flex-development/kustomzcore'

/**
 * @file Subdomain Interface - Playlist Settings
 * @module subdomains/cms/models/PlaylistSettings/interface
 */

export interface IPlaylistSettings extends IEntity {
  url: string
}
